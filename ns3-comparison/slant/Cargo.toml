[package]
name = "flood-benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
slant = { path = "../../lib", features = ["disable_internal_events"] }
az = "1.3.0"
env_logger = "*"
log = { version = "0.4.29", features = ["release_max_level_info"] }